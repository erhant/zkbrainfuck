pragma circom 2.1.0;

template VM(MEM) {
  // previous state
  signal input i;
  signal input p;
  signal input m[MEM];

  // next state
  signal output _i;
  signal output _p;
  signal output _m[MEM];

  // todo...
  

}